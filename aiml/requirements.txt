# ─── Core ML ──────────────────────────────────────────────────────────────────
torch==2.7.1                    # CPU-only build; do NOT upgrade without re-checking PyG wheels
# torchvision                   # not required by scoring worker
# torchaudio                    # not required by scoring worker
torch-geometric==2.7.0

# torch-scatter and torch-sparse must be installed from the PyG wheel index:
#   pip install torch-scatter torch-sparse \
#       -f https://data.pyg.org/whl/torch-2.7.1+cpu.html
torch-scatter==2.1.2+pt27cpu
torch-sparse==0.6.18+pt27cpu

# ─── Classical ML ─────────────────────────────────────────────────────────────
xgboost
scikit-learn

# ─── Explainability ───────────────────────────────────────────────────────────
shap

# ─── Data Processing ──────────────────────────────────────────────────────────
pandas
numpy

# ─── Graph (in-memory research graph) ─────────────────────────────────────────
networkx

# ─── Graph DB ─────────────────────────────────────────────────────────────────
neo4j                  # async Neo4j driver (AsyncGraphDatabase)

# ─── API Layer ────────────────────────────────────────────────────────────────
fastapi
uvicorn[standard]      # includes websockets + httptools

# ─── Async DB Clients ─────────────────────────────────────────────────────────
redis[hiredis]         # redis.asyncio (async Redis client)
motor                  # async MongoDB driver (Motor)

# ─── Async HTTP (callback to Node.js) ─────────────────────────────────────────
httpx                  # used in worker.py to POST /api/ml/result

# ─── Utilities ────────────────────────────────────────────────────────────────
python-dotenv          # load .env
tqdm
matplotlib
joblib
